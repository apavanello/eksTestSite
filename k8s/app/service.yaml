apiVersion: v1
kind: Service
metadata:
  name: app-web
  namespace: app
  labels:
    app.kubernetes.io/name: app-web
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: app-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30180

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfstate.stale-*
terraform/tfplan-*
*.tfplan

# Kubeconfig gerado pelo módulo eks
terraform/.kube/

# Artefatos de sessão da ferramenta (config.json do workspace é versionado)
.zcode/*
!.zcode/config.json

# Diagramas gerados via MCP (regeneráveis sob demanda)
generated-diagrams/
